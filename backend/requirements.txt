# ML Evaluation Platform Backend Dependencies
# Core FastAPI and web framework
fastapi>=0.104.1
uvicorn[standard]>=0.24.0
pydantic[email]>=2.5.0
python-multipart>=0.0.6
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
python-dotenv>=1.0.0

# Database and ORM
sqlalchemy>=2.0.23
alembic>=1.13.1
# Use psycopg (psycopg3) for better Python 3.13 compatibility
psycopg[binary,pool]>=3.1.8
# Fallback: psycopg2-binary for older Python versions
# psycopg2-binary==2.9.9
asyncpg>=0.29.0

# Task Queue
redis>=5.0.1
celery>=5.3.4

# Logging and monitoring
structlog>=23.2.0

# HTTP client
httpx>=0.25.2

# Image processing and ML
pillow>=10.1.0
numpy>=1.24.0  # More flexible for Python 3.13
opencv-python>=4.8.0  # More flexible version

# ML frameworks (these might need manual installation for Python 3.13)
# torch>=2.0.0  # Comment out if installation fails
# torchvision>=0.15.0  # Comment out if installation fails
# ultralytics>=8.0.0  # Comment out if installation fails
# transformers>=4.30.0  # Comment out if installation fails

# Core ML libraries (more likely to work with Python 3.13)
scikit-learn>=1.3.0
matplotlib>=3.7.0
scipy>=1.11.0

# Development dependencies
pytest>=7.4.3
pytest-asyncio>=0.21.1
pytest-cov>=4.1.0
black>=23.11.0
isort>=5.12.0
flake8>=6.1.0
mypy>=1.7.1
pre-commit>=3.6.0

# Performance testing dependencies
psutil>=5.9.0
memory-profiler>=0.60.0